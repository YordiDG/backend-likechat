package org.yoes.likechatbackend.domain.util.security.token;

import java.security.SecureRandom;
import java.util.Base64;

public class KeyGenerator {


    /*public static void main(String[] args) {
        // Definir la longitud de la clave en bytes
        int keyLengthBytes = 16; // 128 bits

        // Generar una clave aleatoria
        SecureRandom secureRandom = new SecureRandom();
        byte[] keyBytes = new byte[keyLengthBytes];
        secureRandom.nextBytes(keyBytes);

        // Convertir la clave a una cadena Base64 para su fácil almacenamiento y uso
        String key = Base64.getEncoder().encodeToString(keyBytes);

        // Imprimir la clave generada
        System.out.println("Clave secreta generada: " + key);
    }*/
}
